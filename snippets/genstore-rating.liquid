{% render 'genstore-rating-display',
  rating_score: rating_score,
  review_count: review_count,
  icon_type: icon_type,
  font_size: font_size,
  icon_color: icon_color,
  max_count: max_count,
  half_star: half_star,
  spacing: spacing
%}
